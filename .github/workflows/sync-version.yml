name: Sync Version – Ruby SDK

on:
  repository_dispatch:
    types: [sync-version]

permissions:
  contents: write
  pull-requests: write

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository (FIXED)
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.PAT }}

      - name: Load version
        run: |
          echo "VERSION=${{ github.event.client_payload.version }}" >> $GITHUB_ENV

      - name: Create branch
        run: |
          BRANCH="sync-v${VERSION}"
          git checkout -b "$BRANCH"
          echo "BRANCH=$BRANCH" >> $GITHUB_ENV

      - name: Update version.rb
        run: |
          sed -i "s/[0-9]\+\.[0-9]\+\.[0-9]\+/$VERSION/" lib/froala-editor-sdk/version.rb

      - name: Update gemspec dependency
        run: |
          sed -i "s/add_dependency .*\"[0-9]\+\.[0-9]\+\.[0-9]\+\"/add_dependency \"froala-editor-sdk\", \"$VERSION\"/" wysiwyg-editor-ruby-sdk.gemspec

      - name: Commit and push
        run: |
          git config user.name "CI Bot"
          git config user.email "ci@github.com"
          git add .
          git commit -m "chore: sync v${VERSION}"
          git push origin "$BRANCH"
